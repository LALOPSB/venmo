== Venmo - Social Payment Service

1. Clone the repository
2. Initialize the db and run the seeds using `rake db:reset`
3. Run specs using `rspec spec`
4. Start the server using `rails s`

5. Run Postman tests
